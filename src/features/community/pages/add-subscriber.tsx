"use client";

import {
  AddSubscriberHeader,
  SubscriberForm,
} from "@/community/components/subscribers";

export const AddSubscriber = () => {
  return (
    <main>
      <AddSubscriberHeader />
      <SubscriberForm />
    </main>
  );
};
